1 learn about variables,learn about arthematic operation like *-multiply,/-divide,//-floor division for dividend only not in point **-for power, = -assignment 
string *5 it print string 5 times 
2 if + concatenation
3 string to expression we need to convert in int
4 read about 3 function len(),type(),str(),print()
5 learn about variables it store value in variable like a box if another value come it over write other value like if store result=10 ,result=5 it print 5 because it overwrite
6 casting value means when we print a value print(my age is + 24) its show error  can only concatenate str (not "int") to str
7 while take true value and run that value till its condition not met
8 if you want to initialize a string just name sting and use that sign ''
9 python == works on string also 
10 if/else use as nested also 
11 print statement give two value first a string then output like this print('s',s)
12 if we pass anyting inside a function its an argument 
13 break it break the loop continue it skip the line and continue to next line
14for loop while loop range function range(start,end,interval)
15imports import different library 


